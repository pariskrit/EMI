import React from "react";
import ServiceLayoutUI from "components/Modules/ServiceLayoutUI";

export default {
	component: ServiceLayoutUI,
	title: "Components/Service Layout UI",
};

const Template = (args) => <ServiceLayoutUI {...args} />;

export const Questions = Template.bind({});
